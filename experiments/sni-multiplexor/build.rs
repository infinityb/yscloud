extern crate tower_grpc_build;

fn main() {
    // // the core q2 service and types
    // tower_grpc_build::Config::new()
    //     .enable_server(true)
    //     .enable_client(true)
    //     .build(
    //         &["../../pb/sni_multiplexor/multiplexor.proto"],
    //         &["../../pb/sni_multiplexor"],
    //     )
    //     .unwrap_or_else(|e| panic!("protobuf compilation failed: {}", e));
}
